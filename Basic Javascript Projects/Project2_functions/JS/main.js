function myfunction() { // Define a function and name it myfunction
    var message = "I am learning how to use Javascript funtions"; // Define a variable and give
    message += " and operators with The Tech Academy's Javascript course!"; // it a string value
    document.getElementById("Concatenate") .innerHTML = message;  // Put the value of the variable into 
    //the HTML element. Use += operator to concatenate a string
}
