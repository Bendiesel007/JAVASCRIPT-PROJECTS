// main.js
// Sent1 and Sent2 are string values
// window.alert is the method used
// Combine strings using the plus operator
// Concatenate the strings by utilizing +
var sent1 = "This is the beginning of the string";
var sent2 = "and this is the end of the string";
window.alert(sent1 + sent2);

// window.alert expression and statement to display a result after the OK button is clicked on the alert window
window.alert(200 * 2)

// document.write an expression and statement to display a result on the page after the OK button is clicked twice on the alert window
document.write(100 + 100);

// Utilize horizontal line "<hr>" to seperate each document method result
document.write("<hr>")

// Utilize document.write method to concatenate a string using the + operator. Results display on the page after the OK button is clicked twice on the alert window
document.write("\"this is the beginning," + "and this is the end of the string.\"")




